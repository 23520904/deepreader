global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "ai-service"
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["ai-service:8080"]

rule_files:
  - /etc/prometheus/alerts.yml
